# from django.contrib import admin
# from .models import Subscription

# @admin.register(Subscription)
# class SubscriptionAdmin(admin.ModelAdmin):
#     list_display = ('user', 'channel', 'subscribed_at')  # Fields displayed in the admin list view
#     search_fields = ('user__username', 'channel__name')  # Enable search by user and channel name
#     list_filter = ('subscribed_at',)  # Add a filter for subscription date
